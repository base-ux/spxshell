
########
# Convert 'tilde' symbol to HOME path
# Arguments:
#   $1	path to convert
# Return:
#   none
# Input:
#   none
# Output:
#   converted path (or original path if no conversion)
########

tilde_path ()
{
    local _p="$1"

    case "${_p}" in ( '~/'* ) _p="${HOME}/${_p#\~/}" ;; esac
    printf "%s" "${_p}"
}
