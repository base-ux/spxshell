# Print canonical path
canonical_path ()
{
    local _path="$1"
    local _d="${_path%/*}"
    local _f="${_path##*/}"

    test -n "${_path}" || return 1
    _d="$(cd -P -- "${_d:-/}" ; pwd)"
    printf "%s" "${_d}/${_f}"
}
